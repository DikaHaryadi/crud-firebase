import 'package:flutter/material.dart';

class MasterAgama extends StatelessWidget {
  const MasterAgama({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Master Agama'),
      ),
    );
  }
}
