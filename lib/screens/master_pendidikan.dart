import 'package:flutter/material.dart';

class MasterPendidikan extends StatelessWidget {
  const MasterPendidikan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Master Pendidikan'),
      ),
    );
  }
}
